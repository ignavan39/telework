import { createStore } from 'vuex'
import teachers from "@/store/modules/teachers";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    teachers
  }
})
